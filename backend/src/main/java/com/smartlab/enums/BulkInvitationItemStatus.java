package com.smartlab.enums;

public enum BulkInvitationItemStatus {
    VALID,
    REJECTED_INVALID,
    REJECTED_DUPLICATE,
    REJECTED_ALREADY_EXISTS,
    QUEUED,
    SENDING,
    SENT,
    EMAIL_FAILED,
    ACTIVATED
}
